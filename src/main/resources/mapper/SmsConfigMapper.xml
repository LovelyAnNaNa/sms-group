<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.whtt.smsgroup.mapper.SmsConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.whtt.smsgroup.entity.pojo.SmsConfig">
        <result column="config_key" property="configKey" />
        <result column="config_value" property="configValue" />
        <result column="config_explain" property="configExplain" />
        <result column="standby_application" property="standbyApplication" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        config_key, config_value, config_explain, standby_application
    </sql>

</mapper>
